# FRONTEND

Էջեր և layout — ինչ օգտատերը **տեսնում** է (React, HTML, CSS).

- `page.tsx` — landing
- `layout.tsx` — ընդհանուր layout
- `admin/` — admin panel UI
- `order/[token]/` — client payment page UI

Backend-ը՝ `app/api/`, `lib/`, Server Actions.
