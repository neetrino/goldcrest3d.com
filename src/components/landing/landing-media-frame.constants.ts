/**
 * Letterboxing behind images when using `contain` so any aspect ratio looks clean.
 */
export const LANDING_MEDIA_CONTAIN_FRAME_BG = "#ebebeb" as const;

/**
 * Same as above but for full-bleed blocks with light text on top (readable on empty bands).
 */
export const LANDING_MEDIA_CONTAIN_FRAME_BG_FULL_BLEED = "#181610" as const;
