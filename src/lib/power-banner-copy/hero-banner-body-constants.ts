/** Max visible text (plain) for hero description — matches prior textarea budget. */
export const HERO_BANNER_BODY_MAX_PLAIN_TEXT_CHARS = 4000;

/** Room for HTML tags when storing rich text in the same DB text column. */
export const HERO_BANNER_BODY_MAX_STORAGE_CHARS = 12000;
