import type { ModelingTextOverlayLayout } from "@/lib/modeling-slot-copy/modeling-text-overlay-layout";

/** Initial layout when enabling Visual text layout in Admin (mobile hero reference frame). */
export const DEFAULT_POWER_BANNER_MOBILE_HERO_TEXT_LAYOUT: ModelingTextOverlayLayout = {
  title: { xPct: 50, yPct: 36, fontSizePx: 30 },
  body: { xPct: 50, yPct: 62, fontSizePx: 16 },
};
