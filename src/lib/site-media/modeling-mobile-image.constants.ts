/**
 * Mobile modeling card frame — matches `modeling-card.constants` (`aspect-[360/259]` below `sm`).
 */
export const MODELING_MOBILE_ASPECT_WIDTH = 360;
export const MODELING_MOBILE_ASPECT_HEIGHT = 259;

/**
 * Output width for processed mobile assets (~4× design width for sharp displays).
 * Height is derived to preserve aspect ratio.
 */
export const MODELING_MOBILE_OUTPUT_MAX_WIDTH_PX = 1440;

/** WebP quality for processed uploads (balance size vs clarity). */
export const MODELING_MOBILE_WEBP_QUALITY = 88;
