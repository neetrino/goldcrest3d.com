/** Modeling Specialization — framing card titles (Admin clarity: desktop vs mobile). */
export const MODELING_DESKTOP_FRAME_POSITION_LABEL = "Desktop Frame & Position";
export const MODELING_MOBILE_FRAME_POSITION_LABEL = "Mobile Frame & Position";

/** Default framing block title (gallery, hero, etc.). */
export const DEFAULT_FRAME_POSITION_LABEL = "Frame & position";

/** Percent points moved per nudge button press. */
export const IMAGE_FRAMING_FOCUS_STEP = 2;
/** Multiplier step for zoom in/out. */
export const IMAGE_FRAMING_ZOOM_STEP = 0.05;
/** Pointer drag sensitivity: pointer delta px → focus % change. */
export const IMAGE_FRAMING_DRAG_SENSITIVITY = 0.12;
